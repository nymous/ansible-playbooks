- name: "Reload systemd"
  become: true
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Reload PHP-FPM pool service
  become: true
  ansible.builtin.systemd_service:
    name: php{{ nextcloud_php_version }}-fpm-nextcloud.service
    state: reloaded
